import './index.css'

const NotFound = () => {
  const msg = 'Page Not Found'

  return (
    <div className="app-container">
      <h1 className="not-found-text">{msg}</h1>
    </div>
  )
}

export default NotFound
